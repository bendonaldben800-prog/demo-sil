# Status

Completed:

- Global key metadata capture with Accessibility permission gating.
- Frontmost app and best-effort window title enrichment.
- Local SQLite persistence for captured events.
- JSON export of persisted capture sessions.
- Clear-log workflow for local data reset.

Recommended next milestones:

- Add configurable retention policy (for example 7/30/90 days).
- Add upload queue and scheduler (launchd) for centralized admin collection.
- Add signed release packaging and notarization checklist.
